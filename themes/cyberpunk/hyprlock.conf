# Hyprlock theme colors
background {
    monitor =
    path =
    color = rgba(0d0208ee)
    blur_passes = 2
    blur_size = 6
}

input-field {
    monitor =
    size = 280, 48
    outline_thickness = 2
    dots_size = 0.25
    dots_spacing = 0.3
    dots_center = true
    outer_color = rgb(00f0ff)
    inner_color = rgb(1a0a12)
    font_color = rgb(e8f0ff)
    fade_on_empty = true
    placeholder_text = <i>password</i>
    hide_input = false
    position = 0, -80
    halign = center
    valign = center
    rounding = 8
    check_color = rgb(ff2a6d)
    fail_color = rgb(ff2a6d)
}

label {
    monitor =
    text = $TIME
    color = rgb(e8f0ff)
    font_size = 64
    font_family = JetBrainsMono Nerd Font
    position = 0, 80
    halign = center
    valign = center
}

label {
    monitor =
    text = $USER
    color = rgb(00f0ff)
    font_size = 18
    font_family = JetBrainsMono Nerd Font
    position = 0, 20
    halign = center
    valign = center
}
